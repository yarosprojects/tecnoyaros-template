import { IconProps } from "../types/iconProps";

const PlayStation = ({ className = "" }: IconProps) => (
    <i className={`icon-[simple-icons--playstation] ${className}`} />
);

export default PlayStation;